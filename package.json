{
  "name": "portfoy-app",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "engines": { "node": ">=18" },
  "scripts": {
    "build": "cd web && npm install && npm run build",
    "start": "node server/index.js"
  },
  "dependencies": {
    "@firstthumb/tefas-api": "latest",
    "express": "^4.19.2"
  }
}
